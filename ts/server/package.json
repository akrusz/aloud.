{
    "name": "@aloud/server",
    "version": "0.0.1",
    "private": true,
    "type": "module",
    "description": "aloud hosted proxy: stateless LLM/STT/TTS forwarding with OAuth, a credits ledger, and metered billing. Open source (AGPL-3.0) like the rest of aloud — see ts/server/README.md for why that's a feature, not a constraint.",
    "main": "./dist/index.js",
    "scripts": {
        "dev": "tsx watch src/index.ts",
        "start": "tsx src/index.ts",
        "typecheck": "tsc --noEmit -p tsconfig.json",
        "test": "vitest run",
        "test:watch": "vitest"
    },
    "dependencies": {
        "@hono/node-server": "^1.13.0",
        "hono": "^4.6.0",
        "jose": "^5.9.0"
    },
    "devDependencies": {
        "tsx": "^4.19.0",
        "typescript": "^5.6.0",
        "vitest": "^3.0.0"
    }
}
