# Audio
pyaudio>=0.2.14
sounddevice>=0.4.6
webrtcvad>=2.0.10

# STT - whisper.cpp (no PyTorch dependency)
pywhispercpp>=1.0
# Optional: Apple Silicon optimization:
# pip install mlx-whisper
# Optional: original OpenAI Whisper (requires PyTorch):
# pip install openai-whisper

# LLM clients
anthropic>=0.40.0
openai>=1.50.0
httpx>=0.27.0

# TTS - macOS 'say' is default, Piper bundled, others optional:
piper-tts>=1.2.0                           # Piper - fast local neural TTS
pathvalidate>=3.0.0                        # Missing from piper-tts deps
# pip install transformers torch           # Parakeet - high quality neural TTS
# pip install nemo_toolkit[tts]            # Parakeet via NeMo (alternative)
# pip install onnxruntime                  # Parakeet via ONNX (alternative)

# For scipy (used by Parakeet for audio output)
scipy>=1.10.0

# Web interface
flask>=3.0.0
flask-socketio>=5.3.0
simple-websocket>=1.0.0
pywebview>=5.0

# Utilities
pyyaml>=6.0.1
python-dotenv>=1.0.0
numpy>=1.24.0
platformdirs>=4.0.0
